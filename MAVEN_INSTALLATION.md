# Установка Apache Maven в Windows

## ✅ Установка завершена успешно!

Apache Maven 3.9.5 был успешно установлен в вашу систему Windows.

### 📁 Расположение файлов:
- **Maven Home**: `C:\apache-maven\apache-maven-3.9.5`
- **Исполняемые файлы**: `C:\apache-maven\apache-maven-3.9.5\bin`

### 🔧 Настроенные переменные окружения:
- **MAVEN_HOME**: `C:\apache-maven\apache-maven-3.9.5`
- **PATH**: Добавлен путь `C:\apache-maven\apache-maven-3.9.5\bin`

### ✅ Проверка установки:
```bash
mvn -version
```

Ожидаемый вывод:
```
Apache Maven 3.9.5 (57804ffe001d7215b5e7bcb531cf83df38f93546)
Maven home: C:\apache-maven\apache-maven-3.9.5
Java version: 21.0.8, vendor: Oracle Corporation
```

## 🚀 Использование с проектом Bitunix Scalper Bot

### Компиляция проекта:
```bash
mvn clean compile
```

### Запуск приложения:
```bash
mvn spring-boot:run
```

### Или используйте готовый batch файл:
```bash
.\start-with-maven.bat
```

## 📋 Доступные Maven команды:

### Основные команды:
- `mvn clean` - очистка проекта
- `mvn compile` - компиляция исходного кода
- `mvn test` - запуск тестов
- `mvn package` - создание JAR файла
- `mvn install` - установка в локальный репозиторий

### Spring Boot команды:
- `mvn spring-boot:run` - запуск приложения
- `mvn spring-boot:build-image` - создание Docker образа

## 🔧 Устранение проблем:

### Если Maven не найден в новой командной строке:
1. Перезапустите командную строку
2. Или выполните в текущей сессии:
```cmd
set MAVEN_HOME=C:\apache-maven\apache-maven-3.9.5
set PATH=%PATH%;C:\apache-maven\apache-maven-3.9.5\bin
```

### Если нужно обновить переменные окружения глобально:
1. Откройте "Параметры системы" → "Дополнительные параметры системы"
2. Нажмите "Переменные среды"
3. Добавьте или обновите:
   - **MAVEN_HOME**: `C:\apache-maven\apache-maven-3.9.5`
   - **PATH**: Добавьте `%MAVEN_HOME%\bin`

## 📊 Проверка работы Rate Limiting системы:

После запуска приложения доступны следующие эндпоинты:

- **Дашборд**: http://localhost:8080
- **Статус Rate Limiter**: http://localhost:8080/api/rate-limiter/status
- **Информация об API**: http://localhost:8080/api/rate-limiter/info/bitunix
- **Проверка запроса**: http://localhost:8080/api/rate-limiter/check/bitunix

## 🎯 Следующие шаги:

1. **Запустите приложение**: `.\start-with-maven.bat`
2. **Откройте браузер**: http://localhost:8080
3. **Проверьте Rate Limiter**: http://localhost:8080/api/rate-limiter/status
4. **Начните торговлю**: Используйте кнопки на дашборде

## 📝 Примечания:

- Maven автоматически загрузит необходимые зависимости при первом запуске
- Проект настроен на Java 11, но работает с Java 21
- Rate Limiting ограничивает запросы до 7 в секунду
- Все API вызовы автоматически контролируются системой ограничений

Установка завершена! Теперь вы можете использовать Maven для работы с проектом.
